[target.x86_64-unknown-linux-musl]
pre-build = [
    "apt-get update && apt-get install -y libssl-dev pkg-config perl make",
]

[target.aarch64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y libssl-dev pkg-config perl make",
]
